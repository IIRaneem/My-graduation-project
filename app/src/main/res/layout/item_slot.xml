<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="80dp"
    android:layout_marginHorizontal="8dp"
    android:layout_marginVertical="6dp">

    <!-- 1. حالة متاح أو محدد: إظهار الكارت الدائري (شكل الكبسولة) -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cvAvailableContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:cardCornerRadius="40dp"
        app:cardElevation="2dp">

        <LinearLayout
            android:id="@+id/layoutSlotContainer"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center"
            android:background="#8AB682">

            <TextView
                android:id="@+id/tvSlotId"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="A6"
                android:textColor="#FFFFFF"
                android:textSize="22sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tvAvailableText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="متاح"
                android:textColor="#FFFFFF"
                android:textSize="14sp"
                android:textStyle="bold"
                android:layout_marginTop="2dp"/>
        </LinearLayout>
    </androidx.cardview.widget.CardView>

    <!-- 2. حالة محجوز: إظهار صورة السيارة فقط (بدون أي خلفية أو كارت) -->
    <ImageView
        android:id="@+id/ivCar"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitCenter"
        android:src="@drawable/car_top"
        android:visibility="gone" />

    <!-- أيقونة ذوي الاحتياجات الخاصة -->
    <ImageView
        android:id="@+id/ivSpecialNeedsIcon"
        android:layout_width="22dp"
        android:layout_height="22dp"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="4dp"
        android:src="@drawable/disabled"
        app:tint="#FFFFFF"
        android:visibility="gone" />

</FrameLayout>